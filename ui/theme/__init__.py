"""GSCO visual theme — design tokens, global CSS, nav, card primitives.

The reference visual source is https://app.cambridge-gsco.co.uk.
All token values were measured directly via Chrome DevTools MCP and live
in ``ui.theme.tokens``. Do not inline hex/rgba values elsewhere — read
them from the tokens module so drift can be audited in one place.
"""
